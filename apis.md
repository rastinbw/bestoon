/submit/expense
    POST, returns a json
    input: date(optional):date, description:string, amount:long, user(token):string
    output: {status:ok}

/submit/income
    POST, returns a json
    input: date(optional):date, description:string, amount:long, user(token):string
    output: {status:ok}
